# Core dependencies
py-algorand-sdk==2.3.0
requests==2.31.0

# Data & utilities
pyyaml==6.0.1

# Database
SQLAlchemy==2.0.23

# Testing & quality
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1
black==23.12.0
ruff==0.1.8
mypy==1.7.1

# Type hints
types-requests==2.31.0.10
types-PyYAML==6.0.12.11

# Monitoring & alerts
python-telegram-bot==20.3

# Async support
aiohttp==3.9.1

# Utilities
python-dotenv==1.0.0
click==8.1.7

# HTTP requests (for Telegram alerts, RPC calls)
requests==2.31.0

# Query API
Flask==3.0.0
Werkzeug==3.0.1

# Optional dev tooling (notebooks/REPL) is intentionally NOT installed here.
# jupyter pulls in jupyterlab, whose pip dependency resolution backtracks
# through dozens of versions and takes 60-80+ seconds even when it succeeds
# (confirmed both in local Docker builds and suspected as the cause of the
# GitHub Actions "Install dependencies" failure on the test(3.11) job) --
# nothing in src/ or tests/ imports ipython/jupyter, so it has no business
# being a required install for running the bot or its test suite. If you
# want notebooks locally, `pip install ipython jupyter` yourself.
